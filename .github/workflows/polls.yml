name: Polls

on:
  # Schedule makes this bot run in regular intervals, without any interventions
  # This will run every day at 5:30 until stopped
  schedule:
    - cron: "30 5 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    defaults:
      run:
        working-directory: bots/polls/
    steps:
      - uses: actions/checkout@v3
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.2.2"
          install-r: false
      - name: Cache R packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/R/renv
          key: ${{ runner.os }}-r-4.2.2-renv-${{ hashFiles('bots/polls/renv.lock') }}
      - name: Restore R packages
        run: |
          install.packages("renv")
          renv::activate()
          renv::restore()
        shell: Rscript {0}
      - name: Run R scripts
        run:  Rscript uk-polls.R
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"
      - name: Install node packages
        run: npm install -g @nzz/q-cli
      - name: Run Q cli
        run: Q update-item
        env:
          Q_PRODUCTION_SERVER: ${{ secrets.Q_PRODUCTION_SERVER }}
          Q_PRODUCTION_ACCESSTOKEN: ${{ secrets.Q_PRODUCTION_ACCESSTOKEN }}